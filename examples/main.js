/*global define:true requirejs:true*/
/* jshint strict: false */
requirejs.config({
    paths: {
        'keypath': 'keypath'
    }
});

define(['keypath'], function(keypath) {
    console.log('Loading');

});